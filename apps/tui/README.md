# fpath TUI (Phase 3)

Terminal-native file browser. Go + Bubble Tea.

## Planned
- `ft` / `fpath tui` CLI command
- Vim keybindings (j/k, /, y/Y, Space)
- Split-pane file preview with syntax highlighting (chroma)
- Fuzzy file search
- Multi-select and copy path (abs/rel)

Status: placeholder — implementation in Phase 3.
